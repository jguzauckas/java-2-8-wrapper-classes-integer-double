# Basics Check

Make an `Integer` variable that holds the number `150`. Make a `Double` variable that holds the number `123.45`.

With those two variables, print out their values.

Print out the largest and smallest integer values as sentences like `"The largest integer is # and the smallest integer is #"`.

Once you have gotten to just warnings, save the Java file and commit and push your changes via GitHub Desktop.
